<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Receitas Culinárias - UTFPR</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
  <style>
    /* ── Tokens ── */
    :root {
      --cafe-escuro:  #6E3A2C;
      --cafe-medio:   #954534;
      --cafe-quente:  #996851;
      --cafe-claro:   #8e8079;
      --cafe-bege:    #A69A97;
      --cafe-creme:   #f5ede8;
      --cafe-card:    #fdf8f5;
      --cafe-borda:   #ddd0c8;
      --cafe-texto:   #2a1a14;
      --cafe-sidebar: #3b2535;
    }

    /* ── Base ── */
    *, *::before, *::after { box-sizing: border-box; }

    html, body {
      height: 100%;
      margin: 0;
    }

    body {
      background-color: var(--cafe-creme);
      color: var(--cafe-texto);
      font-family: Georgia, 'Times New Roman', serif;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      transition: background-color 0.3s, color 0.3s;
    }

    /* ── Top navbar ── */
    .topbar {
      background-color: var(--cafe-escuro);
      border-bottom: 3px solid var(--cafe-medio);
      padding: 0.6rem 1.2rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      position: sticky;
      top: 0;
      z-index: 1030;
    }
    .topbar-brand {
      color: var(--cafe-creme);
      font-size: 1.2rem;
      text-decoration: none;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .topbar-brand i { color: var(--cafe-quente); }
    .topbar-brand:hover { color: var(--cafe-bege); }
    .topbar-right {
      display: flex;
      align-items: center;
      gap: 1rem;
    }
    .topbar-user {
      color: #e8ddd8;
      font-size: 0.9rem;
      display: flex;
      align-items: center;
      gap: 0.4rem;
    }
    .topbar-user .badge {
      background-color: var(--cafe-quente) !important;
      color: #fff !important;
    }
    .topbar-login {
      color: #e8ddd8;
      text-decoration: none;
      font-size: 0.9rem;
      display: flex;
      align-items: center;
      gap: 0.3rem;
      transition: color 0.2s;
    }
    .topbar-login:hover { color: var(--cafe-bege); }
    .topbar-logout {
      color: #f08080;
      text-decoration: none;
      font-size: 0.9rem;
      display: flex;
      align-items: center;
      gap: 0.3rem;
      transition: color 0.2s;
    }
    .topbar-logout:hover { color: #ff6b6b; }

    /* Botão hamburguer mobile */
    .sidebar-toggle {
      background: none;
      border: none;
      color: var(--cafe-creme);
      font-size: 1.4rem;
      cursor: pointer;
      padding: 0;
      display: none;
    }

    /* ── Layout principal ── */
    .app-body {
      display: flex;
      flex: 1;
    }

    /* ── Sidebar ── */
    .sidebar {
      width: 230px;
      min-width: 230px;
      background-color: var(--cafe-sidebar);
      display: flex;
      flex-direction: column;
      padding: 1.5rem 0 0;
      position: sticky;
      top: 51px;
      height: calc(100vh - 51px);
      overflow-y: auto;
      transition: background-color 0.3s, transform 0.3s;
      z-index: 100;
    }

    .sidebar-section {
      padding: 0.3rem 1rem 0.2rem;
      font-size: 0.68rem;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      color: var(--cafe-bege);
      margin-top: 0.8rem;
    }

    .sidebar-link {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.6rem 1.2rem;
      color: #e8ddd8;
      text-decoration: none;
      font-size: 0.92rem;
      border-left: 3px solid transparent;
      transition: background 0.15s, border-color 0.15s, color 0.15s;
    }
    .sidebar-link i { font-size: 1rem; color: var(--cafe-bege); flex-shrink: 0; }
    .sidebar-link:hover {
      background-color: #4e3048;
      border-left-color: var(--cafe-quente);
      color: #fff;
    }
    .sidebar-link:hover i { color: var(--cafe-quente); }
    .sidebar-link.active {
      background-color: #4e3048;
      border-left-color: var(--cafe-medio);
      color: #fff;
    }
    .sidebar-link.active i { color: var(--cafe-quente); }

    .sidebar-divider {
      border-color: rgba(255,255,255,0.07);
      margin: 0.6rem 1rem;
    }

    /* ── Conteúdo ── */
    .main-content {
      flex: 1;
      min-width: 0;
      padding: 2rem;
      display: flex;
      flex-direction: column;
    }
    .main-content main { flex: 1; }

    /* ── Footer (full-width, fora do app-body) ── */
    .site-footer {
      background-color: var(--cafe-escuro);
      color: var(--cafe-bege);
      padding: 0.8rem 0;
      text-align: center;
      font-size: 0.85rem;
      border-top: 3px solid var(--cafe-medio);
    }

    /* ── Cards ── */
    .card {
      background-color: var(--cafe-card);
      border: 1px solid var(--cafe-borda);
      border-radius: 10px;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .card:hover { transform: translateY(-2px); box-shadow: 0 4px 15px rgba(110,58,44,0.15); }
    .card-header.bg-dark { background-color: var(--cafe-escuro) !important; border-radius: 10px 10px 0 0 !important; }
    .card-img-top { border-radius: 10px 10px 0 0; }
    .card-footer { border-radius: 0 0 10px 10px !important; background-color: rgba(110,58,44,0.04); border-top: 1px solid var(--cafe-borda); }

    /* ── Buttons ── */
    .btn-dark { background-color: var(--cafe-escuro); border-color: var(--cafe-escuro); color: #fff; }
    .btn-dark:hover { background-color: var(--cafe-medio); border-color: var(--cafe-medio); }
    .btn-warning { background-color: var(--cafe-quente); border-color: var(--cafe-quente); color: #fff; }
    .btn-warning:hover { background-color: var(--cafe-medio); border-color: var(--cafe-medio); color: #fff; }
    .btn-primary { background-color: var(--cafe-medio); border-color: var(--cafe-medio); color: #fff; }
    .btn-primary:hover { background-color: var(--cafe-escuro); border-color: var(--cafe-escuro); color: #fff; }
    .btn-danger { background-color: #a03c2e; border-color: #a03c2e; color: #fff; }
    .btn-danger:hover { background-color: #7d2e22; border-color: #7d2e22; color: #fff; }
    .btn-outline-dark { color: var(--cafe-escuro); border-color: var(--cafe-escuro); }
    .btn-outline-dark:hover { background-color: var(--cafe-escuro); color: #fff; }
    .btn-outline-warning { color: var(--cafe-quente); border-color: var(--cafe-quente); }
    .btn-outline-warning:hover { background-color: var(--cafe-quente); color: #fff; }
    .btn-outline-primary { color: var(--cafe-medio); border-color: var(--cafe-medio); }
    .btn-outline-primary:hover { background-color: var(--cafe-medio); color: #fff; }
    .btn-outline-danger { color: #a03c2e; border-color: #a03c2e; }
    .btn-outline-danger:hover { background-color: #a03c2e; color: #fff; }
    .btn-outline-light { color: var(--cafe-creme); border-color: var(--cafe-creme); }
    .btn-outline-light:hover { background-color: var(--cafe-creme); color: var(--cafe-escuro); }
    .btn-secondary { background-color: var(--cafe-claro); border-color: var(--cafe-claro); color: #fff; }
    .btn-secondary:hover { background-color: var(--cafe-bege); border-color: var(--cafe-bege); }

    /* ── Badges ── */
    .badge.bg-warning { background-color: var(--cafe-quente) !important; color: #fff !important; }
    .badge.bg-secondary { background-color: var(--cafe-claro) !important; }
    .badge.bg-info { background-color: var(--cafe-bege) !important; color: #3a2318 !important; }
    .badge.bg-dark { background-color: var(--cafe-escuro) !important; }

    /* ── Tables ── */
    thead.table-dark th { background-color: var(--cafe-escuro); color: var(--cafe-creme); border-color: var(--cafe-medio); }
    .table-striped > tbody > tr:nth-of-type(odd) > * { background-color: #f0e6e0; color: var(--cafe-texto); }
    .table-hover > tbody > tr:hover > * { background-color: #e8d8cf; color: var(--cafe-texto); }

    /* ── Progress ── */
    .progress { background-color: var(--cafe-borda); }
    .progress-bar.bg-warning { background-color: var(--cafe-quente) !important; }

    /* ── Alerts ── */
    .alert-info { background-color: #ede0d8; border-color: var(--cafe-bege); color: var(--cafe-escuro); }
    .alert-danger { background-color: #f0dcd8; border-color: #c9a09a; color: #5a1a12; }

    /* ── Headings ── */
    h1, h2, h3 { color: var(--cafe-escuro); }
    /* Garante texto visível em fundos escuros */
    .card-header.bg-dark h1,
    .card-header.bg-dark h2,
    .card-header.bg-dark h3,
    .card-header.bg-dark h4,
    .card-header.bg-dark h5,
    .card-header.bg-dark h6,
    .card-header.bg-dark i,
    .card-header.bg-dark a { color: #fff !important; }
    .text-warning { color: var(--cafe-quente) !important; }
    .text-muted { color: #7a6a64 !important; }
    .text-info { color: var(--cafe-quente) !important; }

    /* ── Lists ── */
    .list-group-item {
      background-color: var(--cafe-card);
      border-color: var(--cafe-borda);
      color: var(--cafe-texto);
    }

    /* ── Forms ── */
    .form-control, .form-select {
      background-color: var(--cafe-card);
      color: var(--cafe-texto);
      border-color: var(--cafe-borda);
    }
    .form-control:focus, .form-select:focus {
      background-color: var(--cafe-card);
      color: var(--cafe-texto);
      border-color: var(--cafe-quente);
      box-shadow: 0 0 0 0.2rem rgba(153,104,81,0.25);
    }
    .input-group-text { background-color: var(--cafe-sidebar); border-color: var(--cafe-borda); color: #e8ddd8; }
    .form-range::-webkit-slider-thumb { background: var(--cafe-quente); }
    .form-check-input:checked {
      background-color: var(--cafe-quente);
      border-color: var(--cafe-quente);
    }
    .form-text { color: var(--cafe-claro); }

    /* ── Mobile ── */
    @media (max-width: 768px) {
      .sidebar-toggle { display: block; }
      .sidebar {
        position: fixed;
        top: 51px;
        left: 0;
        transform: translateX(-100%);
        height: calc(100vh - 51px);
        z-index: 1020;
      }
      .sidebar.open { transform: translateX(0); }
      .sidebar-backdrop {
        display: none;
        position: fixed;
        inset: 0;
        background: rgba(0,0,0,0.4);
        z-index: 1019;
      }
      .sidebar-backdrop.open { display: block; }
      .main-content { padding: 1rem; }
    }
  </style>
</head>
<body>

  <!-- ── Top bar ── -->
  <header class="topbar">
    <div style="display:flex;align-items:center;gap:0.75rem;">
      <button class="sidebar-toggle" id="sidebarToggle" aria-label="Menu">
        <i class="bi bi-list"></i>
      </button>
      <a class="topbar-brand" href="/">
        <i class="bi bi-cup-hot-fill"></i> Receitas UTFPR
      </a>
    </div>
    <div class="topbar-right">
      {{#if logado}}
        <span class="topbar-user">
          <i class="bi bi-person-circle"></i> {{loginUsuario}}
          {{#if admin}}<span class="badge">Admin</span>{{/if}}
        </span>
        <a class="topbar-logout" href="/logout">
          <i class="bi bi-box-arrow-right"></i> Sair
        </a>
      {{else}}
        <a class="topbar-login" href="/login">
          <i class="bi bi-box-arrow-in-right"></i> Entrar
        </a>
      {{/if}}
    </div>
  </header>

  <!-- Backdrop mobile -->
  <div class="sidebar-backdrop" id="sidebarBackdrop"></div>

  <div class="app-body">

    <!-- ── Sidebar ── -->
    <aside class="sidebar" id="sidebar">

      <span class="sidebar-section">Navegação</span>
      <a class="sidebar-link" href="/"><i class="bi bi-house"></i> Início</a>
      <a class="sidebar-link" href="/relatorio"><i class="bi bi-bar-chart"></i> Relatório</a>

      {{#if logado}}
        <hr class="sidebar-divider">
        <span class="sidebar-section">Minhas Páginas</span>
        <a class="sidebar-link" href="/receitas/minhas"><i class="bi bi-journal-text"></i> Minhas Receitas</a>
        <a class="sidebar-link" href="/receitas/cadastrar"><i class="bi bi-plus-circle"></i> Nova Receita</a>
        <a class="sidebar-link" href="/habilidades/minhas"><i class="bi bi-star"></i> Minhas Habilidades</a>
      {{/if}}

      {{#if admin}}
        <hr class="sidebar-divider">
        <span class="sidebar-section">Administração</span>
        <a class="sidebar-link" href="/alunos"><i class="bi bi-people"></i> Alunos</a>
        <a class="sidebar-link" href="/categorias"><i class="bi bi-tags"></i> Categorias</a>
        <a class="sidebar-link" href="/habilidades"><i class="bi bi-lightning"></i> Habilidades</a>
      {{/if}}
    </aside>

    <!-- ── Conteúdo ── -->
    <div class="main-content">
      <main>
        {{{body}}}
      </main>
    </div>

  </div><!-- /app-body -->

  <!-- Footer full-width -->
  <footer class="site-footer">
    <p class="mb-0">&copy; 2026 - Portfólio de Receitas Culinárias | UTFPR - Programação Web Back-End</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // ── Sidebar mobile ──
    const sidebar         = document.getElementById('sidebar');
    const sidebarToggle   = document.getElementById('sidebarToggle');
    const sidebarBackdrop = document.getElementById('sidebarBackdrop');

    sidebarToggle.addEventListener('click', () => {
      sidebar.classList.toggle('open');
      sidebarBackdrop.classList.toggle('open');
    });
    sidebarBackdrop.addEventListener('click', () => {
      sidebar.classList.remove('open');
      sidebarBackdrop.classList.remove('open');
    });

    // ── Marca link ativo ──
    document.querySelectorAll('.sidebar-link').forEach(link => {
      if (link.getAttribute('href') === window.location.pathname) {
        link.classList.add('active');
      }
    });
  </script>
</body>
</html>
