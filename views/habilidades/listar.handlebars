<div class="d-flex justify-content-between align-items-center mb-4">
  <h1><i class="bi bi-lightning text-warning"></i> Gerenciar Habilidades</h1>
  <a href="/habilidades/cadastrar" class="btn btn-warning">
    <i class="bi bi-plus-circle"></i> Nova Habilidade
  </a>
</div>

{{#if erro}}
  <div class="alert alert-danger"><i class="bi bi-exclamation-triangle"></i> {{erro}}</div>
{{/if}}

{{#if habilidades.length}}
  <div class="table-responsive">
    <table class="table table-striped table-hover">
      <thead class="table-dark">
        <tr>
          <th>#</th>
          <th>Nome</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        {{#each habilidades}}
          <tr>
            <td>{{this.id}}</td>
            <td><strong>{{this.nome}}</strong></td>
            <td>
              <a href="/habilidades/editar/{{this.id}}" class="btn btn-outline-primary btn-sm">
                <i class="bi bi-pencil"></i>
              </a>
              <a href="/habilidades/excluir/{{this.id}}" class="btn btn-outline-danger btn-sm"
                 onclick="return confirm('Tem certeza que deseja excluir esta habilidade?')">
                <i class="bi bi-trash"></i>
              </a>
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
{{else}}
  <div class="alert alert-info">
    <i class="bi bi-info-circle"></i> Nenhuma habilidade cadastrada.
  </div>
{{/if}}
